_.merge(cardFundBuilder.routeConfig,{
    list: {
        template: '<apt-panel><apt-card-fund-list autoload="false" edit-conf="{stay: false}" add-new-conf="{stay: false}"></apt-card-fund-list></apt-panel>'
    }
});
